name: hpc

# Stuff to install that isn't available on conda, for some reason:
# - Julia
# - npm install -g pyright, for Microsoft's Python language server

channels:
    - conda-forge
    - defaults
    - bioconda

dependencies:
    - mamba

    # Software to make life nicer
    # Currently emacs is removed because the conda build has a bug
    # - emacs
    - fd-find
    - fzf
    - git
    - pandoc
    - ripgrep
    - tldr
    - tmux
    - zoxide
    # Fix for https://github.com/conda-forge/rabbitmq-server-feedstock/issues/14
    - conda-forge::ncurses

    # Python tooling
    - black
    - ipywidgets
    - isort
    - jupyterlab
    - nb_conda_kernels
    - pip
    - pynvim
